spring:
  application:
    name: mmb-config-client
  cloud:
    config:
      uri:  http://localhost:8001

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
---
server:
  port: 8002